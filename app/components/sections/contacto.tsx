export default function Contacto() {
 // import SocialLink from '../ui/socialLinkBtn'; 
    // Contenido comentado temporalmente para evitar errores
    // return (
    //   <section>
    //     {/* Contacto content */
     {/* Redes Sociales */}
               /* <div className="pt-2 border-t border-(--color-border) ">
                    <h4 className="font-bold text-(--color-primary) uppercase tracking-wider mb-4 text-lg">
                        {aboutData.redesTitle}
                    </h4>
                    <div className=" gap-4 flex flex-row items-center">
                        {aboutData.socialLinks.map((social) => (
                            <SocialLink 
                              key={social.platform}
                              href={social.url}
                              icon={social.icon} 
                              label={social.platform}
                            />
                        ))}
                    </div>
    //   </section>
    // );
    return null;
};
